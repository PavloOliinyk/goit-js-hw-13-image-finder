// const url = 'https://restcountries.eu';

// function fetchCountries(searchQuery) {
//   return fetch(`${url}/rest/v2/name/${searchQuery}`).then(res => res.json());
// }

// export default fetchCountries;
